# سجل التغييرات - CHANGELOG

جميع التغييرات الملحوظة في هذا المشروع موثقة هنا.

---

## [2.0.0] - March 6, 2026

### 🆕 الميزات الجديدة (New Features)

#### نظام الإعدادات العامة (Global Settings)
- **هامش الربح الديناميكي**: يمكن تعديله من لوحة التحكم (الافتراضي: 22%)
- **سعر صرف العملة**: USD → SAR قابل للتعديل (الافتراضي: 3.8)
- **شرائح الاتصال المجانية**: عدد قابل للتعديل (الافتراضي: 2 شرائح)

#### نظام تسعير السيارات الجديد (Car Pricing)
تسعير جديد بناءً على سعة الركاب الإجمالية:

| عدد الركاب | السعر اليومي |
|-----------|-------------|
| 1-3       | $100        |
| 4-6       | $120        |
| 7-8       | $160        |
| 9-12      | $250        |
| 13-24     | $550        |
| 25-45     | $700        |

#### نظام عروض الفنادق (5 مستويات)
هيكل تسعير جديد يدعم 5 مستويات من العروض:
- **Tier 1**: العروض الاقتصادية
- **Tier 2**: العروض القياسية
- **Tier 3**: العروض المتوسطة
- **Tier 4**: العروض الفاخرة
- **Tier 5**: العروض الأكثر فخامة

كل فندق يحتوي على 4 أعمدة تسعير:
- `DBL_View`: غرفة مزدوجة مع إطلالة
- `DBL_NoView`: غرفة مزدوجة بدون إطلالة
- `TRBL_View`: غرفة ثلاثية مع إطلالة
- `TRBL_NoView`: غرفة ثلاثية بدون إطلالة

#### محرك توزيع الغرف الذكي (Smart Room Allocation)
خوارزمية ذكية لتوزيع الركاب على الغرف:

```
Effective Pax = البالغين + الأطفال فوق 6 سنوات

أولوية التوزيع:
1. الغرف الثلاثية (3 أشخاص) أولاً
2. الغرف المزدوجة (2 أشخاص) ثانياً
3. الغرف المفردة فقط إذا لزم الأمر
```

#### محرك حساب التكلفة (Calculation Engine)
معادلة الحساب الكاملة:

```
1. تكلفة الفنادق = مجموع (سعر الغرفة × الليالي) لكل مدينة
2. تكلفة السيارة = السعر اليومي × عدد الأيام
3. تكلفة الخدمات = (إجمالي الركاب - 2 مجانية) × $15
4. التكلفة الأولية = الفنادق + السيارة + الخدمات
5. مع الربح = التكلفة الأولية × 1.22
6. بالريال = مع الربح × 3.8
7. التقريب = تقريب للعشرة الأقرب
```

### 📊 تحديثات قاعدة البيانات (Database Updates)

#### جدول system_settings
```sql
ALTER TABLE system_settings ADD COLUMN:
- base_currency TEXT DEFAULT 'USD'
- exchange_rate_usd_to_sar NUMERIC DEFAULT 3.8
- free_sim_cards_allowance INTEGER DEFAULT 2
```

#### جدول car_pricing (جديد)
```sql
CREATE TABLE car_pricing (
  id UUID PRIMARY KEY,
  min_pax INTEGER NOT NULL,
  max_pax INTEGER NOT NULL,
  price_per_day NUMERIC NOT NULL,
  description_ar TEXT,
  description_en TEXT
);
```

#### جدول hotel_offers (جديد)
```sql
CREATE TABLE hotel_offers (
  id UUID PRIMARY KEY,
  offer_tier offer_tier_type NOT NULL,
  city TEXT NOT NULL,
  hotel_name TEXT NOT NULL,
  dbl_view NUMERIC NOT NULL,
  dbl_no_view NUMERIC NOT NULL,
  trbl_view NUMERIC NOT NULL,
  trbl_no_view NUMERIC NOT NULL
);
```

### 🎨 تحديثات الواجهة (UI Updates)

#### صفحات جديدة
- **صفحة عروض الفنادق** (`/hotel-offers`): إدارة العروض بـ 5 مستويات مع تبويبات
- **صفحة تسعير السيارات** (`/car-pricing`): تعديل أسعار السيارات حسب عدد الركاب

#### تحديث صفحة الإعدادات
- إضافة حقل سعر صرف العملة
- إضافة حقل عدد شرائح الاتصال المجانية
- تحديث هامش الربح الافتراضي إلى 22%

### 📁 الملفات المضافة

```
src/lib/
├── roomAllocation.ts     # خوارزمية توزيع الغرف الذكية
└── calculationEngine.ts  # محرك حساب التكلفة الشامل

src/pages/
├── CarPricingPage.tsx    # صفحة تسعير السيارات
└── HotelOffersPage.tsx   # صفحة عروض الفنادق

src/test/
└── calculationEngine.test.ts  # اختبارات الوحدة

supabase/migrations/
└── 20260306120000_luxury_world_update.sql  # Migration جديد
```

### ✅ الاختبارات (Tests)
- اختبارات وحدة لخوارزمية توزيع الغرف
- اختبارات وحدة لمحرك حساب التكلفة
- اختبارات تكاملية للمعادلات الكاملة

### 📋 البيانات المُدخلة (Seeded Data)

#### بيانات الفنادق (35 سجل)
- 7 مدن × 5 مستويات عروض
- المدن: Tbilisi, Batumi, Kutaisi, Borjomi, Gudauri, Bakuriani, Dashbash

#### بيانات السيارات (6 فئات)
- تسعير لـ 6 نطاقات من عدد الركاب (1-45 راكب)

---

## [1.0.0] - March 6, 2026

### الإصدار الأولي
- إنشاء المشروع الأساسي
- نظام إدارة الفنادق الأساسي
- نظام إدارة السيارات بأنواعها
- نظام المطارات والمسارات
- إعدادات النظام الأساسية

---

## سياسة التوثيق

> **تنبيه هام**: يجب تحديث هذا الملف مع كل commit يتم دفعه إلى GitHub.
> 
> عند كل تحديث، يجب توثيق:
> 1. التاريخ الدقيق للتحديث
> 2. الميزات المُضافة أو المُعدلة
> 3. شرح تفصيلي للتغييرات
> 4. أي تأثيرات على قاعدة البيانات
> 5. ملفات الكود المُتغيرة
